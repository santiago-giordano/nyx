// @see https://www.electron.build/configuration/configuration
{
  $schema: 'https://raw.githubusercontent.com/electron-userland/electron-builder/master/packages/app-builder-lib/scheme.json',
  appId: 'nyxaiapp',
  asar: true,
  productName: 'Nyx',
  directories: {
    output: 'release/${version}',
  },
  publish: [
    {
      provider: 'github',
      owner: 'santiago-giordano',
      repo: 'alice',
      releaseType: 'release',
    },
  ],
  files: ['dist', 'dist-electron'],
  extraResources: [
    {
      from: './app-config.json',
      to: 'app-config.json',
    },
    {
      from: 'resources/backend',
      to: 'backend',
      filter: ['**/*'],
    },
    {
      from: 'backend/lib',
      to: 'backend/lib',
      filter: ['**/*'],
    },
    {
      from: 'backend/models',
      to: 'backend/models',
      filter: ['**/*'],
    },
  ],
  mac: {
    target: ['dmg'],
    icon: 'public/app_logo.png',
    entitlements: 'build/entitlements.mac.plist',
    entitlementsInherit: 'build/entitlements.mac.plist',
    extendInfo: {
      NSMicrophoneUsageDescription:
        'Nyx uses the microphone for voice activity detection and speech recognition.',
    },
    artifactName: 'Nyx-Mac-${version}-Installer.${ext}',
  },
  win: {
    target: [
      {
        target: 'nsis',
        arch: ['x64'],
      },
    ],
    artifactName: 'Nyx-Windows-${version}-Setup.${ext}',
  },
  nsis: {
    oneClick: true,
    perMachine: false,
    allowToChangeInstallationDirectory: false,
    deleteAppDataOnUninstall: false,
  },
  compression: 'store',
  linux: {
    target: ['AppImage'],
    category: 'Utility',
    icon: 'public/app_logo.png',
    artifactName: 'Nyx-Linux-${version}.${ext}',
  },
}
